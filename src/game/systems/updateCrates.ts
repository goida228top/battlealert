import { GameEngine } from '../GameEngine';
import { Crate, Vector2 } from '../types';

export function updateCrates(this: GameEngine, dt: number, timestamp: number): void {
  // Crates/barrels disabled per request
  return;
}
